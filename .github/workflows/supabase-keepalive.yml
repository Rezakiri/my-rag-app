name: Keep Supabase alive

on:
  schedule:
    - cron: "0 0 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase API
        run: |
          curl -s -o /dev/null -w "Status: %{http_code}\n" \
            --header "apikey: sb_publishable_CrXW5LX-cp4Zxt11OGNZNg_urFFcw9F" \
            "https://hmnukjnqjpkzoztymnwz.supabase.co/rest/v1/"
